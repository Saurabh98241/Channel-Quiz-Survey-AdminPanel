@model BusinessLogic.EntityClass.QuizEntity

@{
    ViewBag.Title = "DeleteQuiz";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <div class="box box-primary">
            <div class="box-header">
                <h3 class="box-title">Are you sure you want to delete this quiz?</h3>
            </div>
            <div class="form-horizontal Padding10">
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.QuizName)
                    </dt>
                    <dd>
                        @Html.DisplayFor(model => model.QuizName)
                    </dd>
                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-primary" />
                        @Html.ActionLink("Back to List", "Quiz", "Quiz", new { @class = "btn btn-danger" })
                    </div>
                }
            </div>
        </div>
    </div>
</div>